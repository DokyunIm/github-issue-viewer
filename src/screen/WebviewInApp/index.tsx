import React from 'react';
import SafeAreaView from '@component/common/SafeAreaView';
import {CText} from '@component/common/Text';

function WebviewInApp() {
  return (
    <SafeAreaView>
      <CText>인앱 웹뷰 화면</CText>
    </SafeAreaView>
  );
}

export default WebviewInApp;
